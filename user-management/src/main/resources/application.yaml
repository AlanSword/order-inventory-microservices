spring:
  application:
    name: user-management
  datasource:
    url: jdbc:postgresql://postgres-userroles:5432/userRoles
    username: postgres
    password: 12345678
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/